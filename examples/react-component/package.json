{
  "name": "react-component",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index",
  "scripts": {
    "server": "libpack --entry app.jsx --output ./build --server",
    "start": "libpack --externals react,react-dom --entry index.jsx --output ./build --watch",
    "build": "NODE_ENV=production libpack --externals react,react-dom --entry index.jsx --output ./build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies":{
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  }
}
